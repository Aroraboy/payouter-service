{
  "name": "payouter-service",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc",
    "test": "jest",
    "prepare": "node -e \"require('fs').mkdirSync('data', { recursive:true }); if(!require('fs').existsSync('data/invoices.json')) require('fs').writeFileSync('data/invoices.json','[]')\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.4",
    "@types/node": "^20.8.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
