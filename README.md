# Payouter Service (Final)

Minimal Node.js + TypeScript service integrating with Payouter.

## Setup

1. Create project folder and files (as provided).
2. Install dependencies:

```bash
npm install
```
